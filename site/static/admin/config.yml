backend:
  name: git-gateway
  branch: main # 確保這裡是 main 分支

# 圖片上傳設定
media_folder: "site/static/images/uploads" # 圖片存放在這裡
public_folder: "/images/uploads"           # 文章中讀取圖片的路徑

# 這是最重要的部分：定義你的分類
collections:
  # 1. 玩具格納庫 (Toys)
  - name: "toys" # 對應 content/toys 資料夾
    label: "🧸 玩具格納庫"
    folder: "site/content/toys" # 告訴 CMS 檔案存在哪
    create: true # 允許建立新文章
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}" # 檔名格式
    fields: # 編輯文章時的欄位
      - {label: "標題", name: "title", widget: "string"}
      - {label: "發布日期", name: "date", widget: "datetime"}
      - {label: "封面圖片", name: "image", widget: "image", required: false}
      - {label: "簡短摘要", name: "summary", widget: "text", required: false} 
      - {label: "內容", name: "body", widget: "markdown"}

  # 2. 論文研讀 (Papers)
  - name: "papers" # 對應 content/papers 資料夾
    label: "📄 論文研讀"
    folder: "site/content/papers"
    create: true
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}"
    fields:
      - {label: "論文標題", name: "title", widget: "string"}
      - {label: "研讀日期", name: "date", widget: "datetime"}
      - {label: "相關圖片", name: "image", widget: "image", required: false}
      - {label: "關鍵字/摘要", name: "summary", widget: "text", required: false}
      - {label: "筆記內容", name: "body", widget: "markdown"}

  # 3. 如果你想保留「關於我」的編輯功能，可以留著這個單頁設定
  # 如果不需要，這整段 files 可以刪除
  # - name: "pages"
  #   label: "其他頁面"
  #   files:
  #     - file: "site/content/about/_index.md"
  #       label: "關於我"
  #       name: "about"
  #       fields:
  #         - {label: "標題", name: "title", widget: "string"}
  #         - {label: "內容", name: "body", widget: "markdown"}